import { PatternPage, createPatternMetadata } from "../pattern-page";

export const metadata = createPatternMetadata("coherent");

export default function CoherentBreathingPage() {
  return <PatternPage slug="coherent" />;
}
